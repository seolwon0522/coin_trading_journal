[phases.setup]
nixPkgs = ["jdk17", "gradle"]

[phases.build]
cmds = ["cd backend && chmod +x gradlew && ./gradlew build -x test --no-daemon"]

[start]
cmd = "cd backend && java -Xmx512m -Dserver.port=${PORT:-8080} -Dspring.profiles.active=railway -jar build/libs/trading-bot.jar"